{
  "name": "cbt-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "set NODE_ENV=test&& jest --runInBand --detectOpenHandles",
    "test:watch": "set NODE_ENV=test&& jest --watch --runInBand",
    "test:coverage": "set NODE_ENV=test&& jest --coverage --runInBand",
    "test:unit": "set NODE_ENV=test&& jest tests/unit --runInBand",
    "test:integration": "set NODE_ENV=test&& jest tests/integration --runInBand",
    "test:e2e": "set NODE_ENV=test&& jest tests/e2e --runInBand",
    "dev": "nodemon index.js",
    "postinstall": "prisma generate",
    "build": "npx prisma generate",
    "start": "node index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.19.1",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "joi": "^18.0.2",
    "jsonwebtoken": "^9.0.3"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.3.1",
    "jest": "^29.7.0",
    "nodemon": "^3.1.11",
    "prisma": "^6.19.0",
    "supertest": "^6.3.3"
  }
}
